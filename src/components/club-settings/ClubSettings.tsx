import { FC } from 'react';
import { Box } from '@mui/material';
import ContentWrapper from '../content-wrapper/ContentWrapper.tsx';

const ClubSettings: FC = () => {
    return (
        <ContentWrapper>
            <Box>ClubSettings</Box>
        </ContentWrapper>
    );
};

export default ClubSettings;