import { StylesTheme } from '../../../../types/mui.ts';

const styles: StylesTheme = {
    root: {
        marginTop: '12px'
    },
    tabWrapper: {
        borderBottom: 1,
        borderColor: 'divider'
    }
};

export default styles;
